# 우리집 가계부 — 제안 기능 백로그

> "그거 하자" 하면 꺼내 쓰는 아이디어 모음. 어느 기기·계정에서든 보이게 이 파일(레포/OneDrive)에 남긴다.
> 최종 갱신: 2026-09-09 (8/18~19 개선·보안감사, 9/7 할부 표시까지 반영)

---

## ✅ 이미 구현·배포된 것 (2026-07-28 세션)
- **거래 검색** — 목록 상단에서 최근 1년(로드 13개월) 이름·메모·카테고리·금액 검색
- **월말 지출 예측 보강** — 남은 일수 + 전월 대비 예상 증감액
- **소프트 삭제 + 휴지통 30일** — 삭제해도 되살리기, 30일 후 자동 완전삭제(사진 포함)
- **사용자 카테고리 추가** — 설정 › 카테고리 관리(이모지 골라 추가/삭제)
- **JSON 백업/복원(사진 포함)** — Excel이 못 담는 영수증까지 한 파일로. 복원은 중복 없이 추가
- **금액 천단위 콤마 입력**, FAB 크기 축소, 라벨 정리(로그아웃 / 제미나이 API 키 입력)
- **카드사 + 무이자 할부** — 거래 등록 시 카드사(선택)와 할부 개월(2~24) 지정.
  할부면 '금액'을 총액으로 보고 N개월치를 각 달에 미리 만들어 같은 그룹으로 묶는다 → **다음 달에도 계속 표시**되고
  목록엔 `할부 3/6` 배지, 상세엔 남은 회차·남은 금액, [이 할부 전체 취소]로 남은 회차까지 한 번에 휴지통으로.
  카드 문자를 붙여넣으면 **카드사·할부 개월도 자동 인식**(일시불은 할부로 오인하지 않음). Excel에도 카드/할부 열 추가.

## ✅ 이미 구현·배포된 것 (2026-08-07~08 세션)
> 이 중 개인 계정·전체 기간 검색·크림 테마는 다른 계정 세션에서 만든 것(커밋 안 된 채 배포돼 있던 것을 이어받아 커밋함).
- **개인 계정** — 각자 이름+비밀번호로 로그인. 예전 공유 비밀번호는 '가입 코드'가 되어 계정 만들 때만 쓴다.
  비밀번호는 PBKDF2-SHA256(150,000회) 해시만 저장. 가계부 데이터는 그대로 공유.
- **전체 기간 검색** — 서버 `/api/search`로 D1 전체를 검색(예전엔 앱이 받아둔 1년치 안에서만).
- **크림(미색) 테마 기본**, 거래추가 4칸 색 구분, 자동 키패드 제거, 월 라벨에 '· 이번 달'.
- **카테고리 편집** — 거래추가 화면의 [✏️ 편집·추가](설정에도 있음)에서 이름·이모지 변경, 숨기기,
  순서 이동, 삭제. 즉시 서버 저장 → 두 폰 공유. **key(원래 이름)는 안 바꾸므로 과거 기록이 안전**하고,
  숨겨도 과거 거래엔 계속 보인다. ('기타/기타수입'은 폴백이라 숨김·이동 금지, 늘 맨 끝)
- **카드 결제일·이용기간** — 카드는 '쓴 달'과 '통장에서 빠지는 달'이 다르다.
  규칙: 결제월 M, 시작일 S, 결제일 P → 이용기간 (M-1)월 S일 ~ M월 (S-1)일, 결제 M월 P일.
  홈에 '이번 달 나갈 카드값' 한 줄 + 상세(카드별 결제일·이용기간·건수·금액), 설정에서 카드별 수정·추가·삭제.
  기본값: 하나 8/20, BC 11/23, 국민·농협·우리 12/25, 삼성 13/25, 현대 14/25 (농협 25일 확인받음).
  저장은 app_settings의 `card_settings`(스키마 변경 없음), 백업/복원에도 포함.
- **제미나이 키 진단 정정** — 구글이 일부 계정에 주는 `AQ.` 키는 API 키로 받아주지 않는다(3가지 인증
  방식 모두 401, 구글 포럼에도 다수 신고). 예전 안내가 "잠시 후 다시 시도"라 헛수고를 반복하게 만들었다.
  이제 붙여넣는 즉시 형식을 알려주고, 안 되는 이유와 AIza 키 받는 법을 알려준다. **실키로 작동 확인됨.**
- **정기지출: 노란우산공제** 매월 25일 50,000원 등록(카테고리 기타).
- 상단 🔄의 '동기화' → **'새로고침'**으로 문구 정정(무슨 기능인지 안 통했음).
- **결제월 기준 보기** — 카드마다 주기가 달라도 거래 하나하나는 어느 한 결제월에 속한다(시작일 이후=다음 달).
  홈에 '🏦 이번 달 나갈 돈'(쓴 돈과 차액·카드/현금 분리), 카테고리 막대에 [쓴 기준 | 나갈 기준] 토글.
  전월 대비도 같은 기준으로 센다. 합계 보존 검증 완료(중복·누락 0).
- **웹푸시 알림** — 거래·할부가 등록되면 알림 켠 기기 전부에 알림. VAPID 키는 Cloudflare secret,
  마이그레이션 005 push_subscriptions. 페이로드 없는 '깨우기' 푸시 + 서비스워커가 /api/push/latest로
  문구를 만든다("아내 · 이마트 (국민) / ₩94,500 · 마트"). 죽은 구독 자동 삭제(애플의 400 BadWebPushToken 포함).
  설정에 [새 거래 알림] 토글 + [테스트 알림 보내기](기기별 응답 코드까지 보여주는 진단).
  **아이폰은 홈 화면 설치 상태에서만 동작**(애플 제약) — 설치 전엔 안내 문구를 띄운다.
- **숫자 키패드** — 금액·일자칸에 `pattern="[0-9]*"`. iOS는 inputmode만으론 QWERTY가 뜬다.
- **개인 계정 가입 코드 = `money`** (2026-08-08 설정). 가입할 때만 쓰이고 로그인엔 안 쓰인다.

## ✅ 이미 구현·배포된 것 (2026-08-18 세션)
- **총예산 + 남은 금액** — 카테고리를 하나하나 정하지 않아도 '이번 달 얼마까지'만 정하면 남은 돈이 보인다.
  예산 화면에서 설정·진행 막대, 홈에 '🎯 총예산 중 ○○원 남음'. app_settings.total_budget(부부 공유), 백업에도 포함.
- **정기 '수입' 자동등록** (마이그레이션 007 `recurring_rules.type`) — 급여처럼 매달 들어오는 수입도 자동 등록.
  예전엔 자동등록이 'expense'로 박혀 있어 지출만 됐다. 정기 모달에 지출/수입 토글.
- **금액 변동 정기지출** (마이그레이션 006 `variable`) — 관리비처럼 날짜는 같은데 금액이 다른 것.
  자동으로 넣지 않고 그날 "📝 관리비 금액을 입력해 주세요 (지난번 ₩150,000)" 알림만 보낸다.
- **중복 입력 감지** — 같은 날·같은 금액·같은 카테고리가 이미 있으면 되묻는다(문자 자동 + 손입력 이중 등록 방지).
- **백업 알림** — 마지막 백업일을 서버에 기록해, 30일이 넘거나 한 번도 안 했으면 홈에 안내 한 줄.
- **매일 기록 알림을 서버 cron으로** — 앱 안 타이머는 앱이 꺼지면 멈추고 iOS는 `new Notification()`을
  아예 지원하지 않아 영영 안 떴다. 이제 Cloudflare cron(*/10)이 시각을 보고 웹푸시로 보낸다.
- **새 버전 알림** — 배포되면 '✨ 새 버전이 나왔어요' 배너. 상단바에 **🔄(데이터 새로고침) / ⬇️(앱 최신 버전)**
  두 버튼을 아이콘으로 구분. `[version_metadata]`로 배포마다 자동 버전(손으로 번호 올릴 필요 없음).
- **동백전(선불) 지원** — 선불이라 '쓰는 즉시' 빠진다. 카드값 목록에 안 섞이고 결제월은 쓴 달 그대로.
  카드 선택칸을 [설정 › 카드값] 등록 카드로 **자동 생성**(예전엔 하드코딩이라 설정에 없는 카드로 기록하면
  결제월 계산이 조용히 틀어졌다).
- **입력칸 가시성** — 금액·카드·할부·날짜가 흰 상자라 어디를 눌러야 할지 안 보였다. 금액은 비어 있을 때부터
  accent 테두리 2px·22px, 선택칸엔 ▾ 화살표(appearance:none이 기본 화살표를 지웠던 것), 날짜도 강화.
- **저장 속도·중복 등록** — 서버가 푸시 발송(애플·구글 왕복)을 기다린 뒤 응답해 저장이 느렸고, 그 사이
  버튼이 계속 눌려 같은 거래가 두세 번 들어갔다. 응답을 먼저 보내고 푸시는 뒤로(ctx.waitUntil) + 버튼 잠금.
  실측 0.08s → 0.01~0.03s. 삭제·복원 등 5곳도 목록 갱신을 기다리지 않게 고침.
- **비밀번호 6자리 이상**(숫자), 예산 기준 통일(홈·예산화면·경고가 같은 기준), 죽은 코드 제거.
- **가입 코드 잠금** — `money` → 무작위 32자. 이 코드는 가입뿐 아니라 **비밀번호 재설정도 통과**시키므로
  사전 단어면 계정을 통째로 뺏길 수 있었다. 새 코드는 `가입코드.txt`(.gitignore, 커밋 안 됨).
- **토큰이 실제 계정을 확인** — 서명만 맞으면 통과라 계정을 지워도 90일 내내 살아 있었다.

## ✅ 이미 구현·배포된 것 (2026-08-19 · 다른 계정 세션)
- **보안·정합성 감사 6건**. 이 중 중요: **백업 복원 시 정기규칙의 `variable`·`type`이 누락**돼,
  복원하면 관리비가 옛 금액으로 자동등록되고 급여가 지출로 뒤집혔다(8/18 기능의 구멍).
  그 외 정기지출 이중탭 방어, 이모지 esc 통일, 복원 개수 상한(각 5만), photo_url 형식·크기(8MB) 서버 검증,
  미인증 500 오류 원문 노출 차단.
- **토큰 무효화**(마이그레이션 008 `users.token_ver`) — 비밀번호 변경·재설정 시 **다른 기기 강제 로그아웃**.
  바꾼 기기는 새 토큰으로 유지. 옛(v 없는) 토큰은 0으로 취급해 배포해도 안 끊긴다.
- **회귀 수정 3건** — 비번 오타 401을 토큰만료로 오인해 로그아웃되던 문제, 마이그레이션 번호 충돌(006→008),
  비번 변경 후 서비스워커 푸시 토큰이 옛것이라 알림 문구가 일반문구로 뜨던 문제.
- **월 1회 전체 백업 이메일** — 매월 1일 09:00 KST cron으로 백업 JSON을 첨부해 dooho0812@gmail.com 발송.
  Resend API 사용(워커는 자체 메일 불가). 18MB 넘으면 사진만 빼고 보냄. 테스트용 `POST /api/backup/email`.
  ⚠️ **`RESEND_API_KEY` 시크릿이 없으면 조용히 건너뛴다 — 현재 미등록이라 꺼져 있음**(아래 '손이 필요한' 참고).

## ✅ 이미 구현·배포된 것 (2026-09-07 · 다른 계정 세션)
- **할부 상세에 '시작월 → 마지막 회차'** — "📅 2026년 8월 시작 → 2026년 12월 마지막 회차" 한 줄.
  이 달 날짜와 회차로 역산하므로 13개월 로드 창 밖의 회차가 없어도 정확하다.

---

## 💡 제안 (미구현) — 값어치·노력 표시

> ~~정기 '수입' 자동등록~~·~~총예산 + 남은 금액~~·~~중복 입력 감지~~ 는 2026-08-18에 구현 완료(위 참고).

### 착수 대기 (결정 끝남)
1. **🏢 사무실 지출 → 동래연제 연동** — 가계부에서 [🏢 사무실용]으로 찍으면 동래연제 앱 💸 지출 탭에 자동 등록.
   집 살림 숫자(홈·카테고리·리포트·예산)에서는 빼되 **카드 청구액에는 포함**, 저장 즉시 자동 전송.
   결정 완료(26-09-08). 작업서: `0.app/_문서/가계부_사무실지출_연동_작업서.md`

### 중간 노력
2. **다가오는 정기지출 예고** — `recurring_rules.day` 활용 → "3일 뒤 관리비 15만원" 알림. 잔고 놀람 방지.
3. **저축 목표** — "여행자금 200만원" 목표 + 진척률 막대. 부부가 같이 보는 재미.
4. **연말 결산** — "올해 총 수입/지출/카테고리 1위" 한 장 요약.
5. **요일·시간대 히트맵** — 데이터 쌓이면 "주말 저녁에 지출 몰림" 패턴 시각화.

### 큰 노력 (설계 필요)
6. **자산/잔액 추적** — 지금은 '흐름(수입/지출)'만. 통장 잔고를 이월해 "지금 우리 돈 얼마"를 관리(플로우→스톡).

---

## 🚫 안 하기로 결정
- **카카오 로그인 (26-07-28)** — 부부 2명이 하나의 가계부를 일부러 공유하는 구조라 공유 비밀번호 → 서명 토큰이 최적.
  외부 의존성·콘솔 설정 없음, 90일이라 재로그인도 드묾. 예전 KOE205로 제거한 이력도 있음.
- **사람별 지출 집계('누가 얼마 썼나') (26-07-28)** — `user_name`은 **돈을 쓴 사람이 아니라 앱에 기록한 사람**이다
  (배우자 결제를 대신 적을 수 있다). 이걸 지출로 집계하면 사실과 다른 화면이 된다.
  거래마다 기록한 사람 이름이 이미 뜨는 것으로 충분. 대신 마감 리포트의 '인별 지출' 표기를
  **'기록한 사람별' + 기준 설명**으로 바로잡았다.

## 🙋 실장님 손이 필요한 것 (개발로는 못 하는 것)
- **⚠️ `RESEND_API_KEY` 등록** — 월 1회 백업 이메일이 **지금 꺼져 있다**(코드는 있는데 키가 없어 조용히 건너뜀).
  [resend.com](https://resend.com) 가입 → API 키 발급 → `npx wrangler secret put RESEND_API_KEY`.
  등록 뒤 `POST /api/backup/email`(앱 로그인 상태)로 월초를 안 기다리고 즉시 시험 가능. 무료 플랜으로 충분.
- **아이폰 실기기 확인(남음)** — JSON 백업 파일이 iOS에서 저장/공유되나(iOS Safari는 blob 다운로드가 특이).
- **카드 첫 청구서와 대조** — 결제월 규칙(시작일 이후=다음 달)이 카드사 실제 마감과 하루 이틀 다를 수 있다.
  다르면 [설정 › 카드값]에서 시작일을 ±1 조정하면 맞는다.

## ✔ 끝난 준비 작업 (기록용)
- **계정** — 두두(26-08-09)·지뇽이(26-08-18) 두 개 생성 완료. 양쪽 아이폰 웹푸시 구독까지 확인됨.
- **가입 코드 잠금** — `money` → 무작위 32자로 교체 완료(26-08-18). 새 값은 `가입코드.txt`(커밋 안 됨).
  가입뿐 아니라 **비밀번호 재설정에도 필요**하니 이 파일을 지우지 말 것.
- **토큰 조이기** — 계정 실재 확인(26-08-18) + 비번 변경 시 옛 토큰 무효화(26-08-19) 둘 다 적용됨.
- **Gemini 영수증 인식** — 실키로 작동 확인됨(26-08-08).
